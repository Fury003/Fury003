# Raw Packaged
import pywhatkit
import time
import pandas as pd

#Data Source
import yfinance as yf

#searched index
Tesla = yf.download(tickers='TSLA', period='5d', interval='5m')

#incerc sa indexez(transform in lista/tuplu/set) ca sa pot sa pun highlighted
print(type(Tesla))

#wait time for debug
#time.sleep(5)

#message itself
#pywhatkit.sendwhatmsg("+40733835338" ,str(Tesla), 15 , 26 , 10 , True , 10)

#incerc tabelul
df = pd.DataFrame(Tesla, index=[list[1]])
print(df)

#E o clasa pandas deja dar eu daca scot pandas in import tot clasa pandas ramane
#tot nu mi iese dataframul si incerc sa aflu val max  dar nu stiu cum s o apelez daca nu e in forma de baza sa zic asa
